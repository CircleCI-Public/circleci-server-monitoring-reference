prometheus:
  persistence:
    enabled: true
    storageClass: "custom-storage"

grafana:
  persistence:
    enabled: true
    storageClass: "custom-storage"

tempo:
  storage:
    traces:
      storageClassName: "custom-storage"
      backend: pv
